import sys


def ConnectZ(object):
    def __init__(self, file_input=None):
        # Will always need a file
        pass

if __name__ == "__main__":
    if len(sys.argv) != 2:
        # Incorrect command line argument count. Alert user
        sys.stdout.write('connectz.py: Provide one \033[92minput \033[0mfile')  # Assumes ANSI available (Linux default)

